# BFS Coverage Implementation - Статус

## Дата: 2025-12-14

## Что было сделано

### 1. Combined Score для выбора семьи ✅
- Реализовано взвешивание структурного и персонального score
- 40% структура + 30% муж + 30% жена
- **Результат**: Правильный выбор семей при множественных кандидатах

### 2. Добавление несопоставленных членов семьи в очередь ✅ (Частично)
- Создан метод `ProcessMatchedFamily()` который собирает всех членов семьи
- Добавляет несопоставленных членов в очередь для исследования
- **Но**: Несопоставленные персоны пропускаются в основном цикле

## Текущая проблема

### Код на строках 136-148:

```csharp
// Проверяем, сопоставлена ли персона
if (!mappings.ContainsKey(currentSourceId))
{
    // Персона НЕ сопоставлена, но мы добавили её для исследования потомков
    _logger.LogDebug(
        "Processing unmatched person {PersonId} at level {Level} for exploration only",
        currentSourceId,
        level);

    // Для несопоставленной персоны мы НЕ можем обрабатывать её семьи,
    // так как у нас нет соответствующих destination families.
    // Пропускаем её - она уже в processed, поэтому не будет добавлена снова.
    continue;  // ← ПРОБЛЕМА ЗДЕСЬ
}
```

**Что происходит:**
1. Юрий (@I35@) не был сопоставлен (score < threshold)
2. Юрий был добавлен в очередь (как несопоставленный член найденной семьи F12)
3. Юрий достигается из очереди на level 4
4. Код видит, что Юрий НЕ в mappings → `continue` → **ПРОПУСКАЕТСЯ**
5. Семья Юрия (F13) и его потомки **НИКОГДА не обрабатываются**

## Возможные решения

### Решение 1: Обработка через сопоставленных родственников (РЕКОМЕНДУЮ)

**Идея:** Для несопоставленной персоны найти dest families через уже сопоставленных членов её семей.

**Пример:**
```
Юрий (@I35@) - НЕ сопоставлен

Семья F13 (как супруг):
  Husband: Юрий @I35@ [NOT MATCHED]
  Wife: Жена Юрия @I??? [UNKNOWN]
  Children: Дети Юрия [UNKNOWN]

Семья F12 (как ребенок):
  Husband: Александр @I27@ [MATCHED → @I6000...540@]
  Wife: Вера @I34@ [MATCHED → @I6000...842@]
  Child: Юрий @I35@ [NOT MATCHED]
```

**Логика:**
1. Юрий достигается из очереди, но не сопоставлен
2. Ищем семьи Юрия в source (F13, F12)
3. Для каждой семьи:
   - Для F12 (как ребенок): родители уже сопоставлены → находим их dest families
   - Для F13 (как супруг): **супруга и детей ищем во ВСЕХ dest families**
4. Пытаемся сопоставить супругу и детей Юрия
5. Если нашли - добавляем в очередь, продолжаем BFS

**Псевдокод:**

```csharp
if (!mappings.ContainsKey(currentSourceId))
{
    _logger.LogDebug("Processing unmatched person {Id} for exploration", currentSourceId);

    // Обрабатываем семьи, где персона — СУПРУГ/РОДИТЕЛЬ
    var sourceFamiliesAsSpouse = TreeNavigator.GetFamiliesAsSpouse(sourceTree, currentSourceId);

    foreach (var sourceFamily in sourceFamiliesAsSpouse)
    {
        // Для несопоставленной персоны dest families найти сложно
        // Но мы можем попробовать сопоставить других членов семьи!

        // Пытаемся найти супруга и детей во ВСЕХ dest families
        var unmatchedMembers = new List<string>();
        if (sourceFamily.WifeId != null && !mappings.ContainsKey(sourceFamily.WifeId))
            unmatchedMembers.Add(sourceFamily.WifeId);
        if (sourceFamily.HusbandId != null && !mappings.ContainsKey(sourceFamily.HusbandId))
            unmatchedMembers.Add(sourceFamily.HusbandId);
        foreach (var childId in sourceFamily.ChildIds)
        {
            if (!mappings.ContainsKey(childId))
                unmatchedMembers.Add(childId);
        }

        // Для каждого несопоставленного члена пытаемся найти соответствие
        foreach (var memberId in unmatchedMembers)
        {
            // Ищем среди ВСЕХ персон destination
            var bestMatch = FindBestGlobalMatch(
                sourceTree.PersonsById[memberId],
                destTree,
                thresholdCalculator.GetThreshold(DetermineRelationType(memberId, sourceFamily), 100));

            if (bestMatch != null)
            {
                // Нашли сопоставление!
                var newMapping = new PersonMapping
                {
                    SourceId = memberId,
                    DestinationId = bestMatch.Id,
                    MatchScore = bestMatch.Score,
                    Level = level + 1,
                    FoundVia = DetermineRelationType(memberId, sourceFamily),
                    FoundInFamilyId = sourceFamily.Id
                };

                mappings[memberId] = newMapping;
                queue.Enqueue((memberId, level + 1));
                processed.Add(memberId);
            }
            else
            {
                // Не нашли - добавляем для дальнейшего исследования
                if (!processed.Contains(memberId))
                {
                    queue.Enqueue((memberId, level + 1));
                    processed.Add(memberId);
                }
            }
        }
    }

    // Аналогично для семей, где персона — РЕБЁНОК
    // (семьи родителей уже найдены, можем работать через них)

    continue; // Переходим к следующей персоне
}
```

**Преимущества:**
- ✅ Позволяет обрабатывать потомков несопоставленных персон
- ✅ Использует глобальный поиск для членов семьи
- ✅ Не требует кардинальных изменений архитектуры

**Недостатки:**
- ⚠️ Глобальный поиск медленнее
- ⚠️ Может найти неправильные сопоставления без структурного контекста
- ⚠️ Сложная логика

### Решение 2: Упростить - не добавлять несопоставленных в очередь

**Идея:** Не добавлять несопоставленных членов семьи в очередь вообще.

**Обоснование:**
- Если человека не удалось сопоставить с хорошим контекстом (найденная семья), то:
  - Либо его нет в destination
  - Либо данные слишком разные
- В обоих случаях обработка его потомков бессмысленна

**Изменения:**
```csharp
// Удалить строки 541-558 в WaveCompareService.ProcessMatchedFamily()
// Больше НЕ добавлять несопоставленных членов в очередь
```

**Преимущества:**
- ✅ Простое решение
- ✅ Минимум изменений
- ✅ Избегаем обработки потенциально несуществующих персон

**Недостатки:**
- ❌ Теряем целые ветки дерева
- ❌ Не решает исходную проблему пользователя

### Решение 3: Добавлять только если есть другие сопоставленные члены семьи

**Идея:** Добавлять несопоставленную персону в очередь ТОЛЬКО если у неё есть **собственная семья с хотя бы одним сопоставленным членом**.

**Пример:**
```
Юрий @I35@ - НЕ сопоставлен

Семья F13 (Юрий как супруг):
  Husband: Юрий @I35@ [NOT MATCHED]
  Wife: Жена @I??? [CHECK IF MATCHED]
  Children: [CHECK IF ANY MATCHED]

Если жена ИЛИ хотя бы один ребенок сопоставлен → добавляем Юрия в очередь
Иначе → НЕ добавляем
```

**Псевдокод:**
```csharp
// В ProcessMatchedFamily, перед добавлением в очередь:
if (anyMemberMatched)
{
    foreach (var unmatchedMemberId in allSourceFamilyMembers)
    {
        if (!processed.Contains(unmatchedMemberId))
        {
            // Проверяем, есть ли у unmatchedMemberId собственные семьи
            // с сопоставленными членами
            if (HasMatchedFamilyMembers(unmatchedMemberId, sourceTree, mappings))
            {
                queue.Enqueue((unmatchedMemberId, level + 1));
                processed.Add(unmatchedMemberId);
                _logger.LogDebug("Added unmatched member {Id} (has matched family)", unmatchedMemberId);
            }
        }
    }
}

bool HasMatchedFamilyMembers(string personId, TreeGraph tree, Dictionary<string, PersonMapping> mappings)
{
    var families = TreeNavigator.GetFamiliesAsSpouse(tree, personId);
    foreach (var family in families)
    {
        if (family.HusbandId != null && mappings.ContainsKey(family.HusbandId)) return true;
        if (family.WifeId != null && mappings.ContainsKey(family.WifeId)) return true;
        if (family.ChildIds.Any(cid => mappings.ContainsKey(cid))) return true;
    }
    return false;
}
```

**Преимущества:**
- ✅ Более разумная эвристика
- ✅ Добавляем только тех, у кого есть шанс на дальнейшую обработку
- ✅ Минимизирует бесполезную работу

**Недостатки:**
- ⚠️ Все еще требует решения проблемы с обработкой несопоставленных персон
- ⚠️ Может пропустить важные ветки

## Рекомендация

**Комбинация Решений 1 и 3:**

1. **Добавлять в очередь** только несопоставленных членов с собственными сопоставленными родственниками (Решение 3)
2. **Обрабатывать** несопоставленных персон через глобальный поиск для их семей (Решение 1)

Это обеспечит:
- ✅ Полноту обхода дерева
- ✅ Разумное использование ресурсов
- ✅ Возможность найти потомков через несопоставленных предков

## Текущий статус

- ✅ **Combined Score**: Реализовано и работает
- ⚠️ **BFS Coverage**: Частично реализовано, но несопоставленные персоны пропускаются
- 📋 **Требуется**: Реализация обработки несопоставленных персон (Решение 1 или 3)

## Результаты тестирования

**С max-level=3:**
- Mapped: 30 persons (как и раньше)
- Юрий @I35@ не найден

**С max-level=5:**
- Mapped: 156 persons (!!!)
- Но Юрий @I35@ все еще не найден (пропускается в цикле)

**Вывод**: Увеличение max-level помогло найти больше людей, но несопоставленные члены семьи (как Юрий) все еще теряются.

---

**Автор**: Claude Sonnet 4.5
**Дата**: 2025-12-14
